<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lu.mydemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TestFunctionActivity"/>
        <activity android:name=".WebPagesActivity" />
        <activity android:name=".WebViewActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".PingjiaoActivity" />
        <activity android:name=".ColorConfigActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".NewsSavedActivity" />
        <activity android:name=".NewsDetailActivity" />
        <activity android:name=".NewsActivity" />
        <activity android:name=".CourseScheduleChangeActivity" />
        <activity android:name=".WeekCourseActivity" />
        <activity android:name=".NoneScoreCourseActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ScoreActivity" />

        <meta-data
            android:name="android.max_aspect"
            android:value="2.5" />
    </application>

</manifest>