<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/tool_bar"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/tool_bar_back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:layout_marginStart="10dp"
        android:src="@drawable/ic_arrow_back_white_24dp"
        android:contentDescription="@string/back"/>

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@+id/tool_bar_back"
        android:layout_toStartOf="@+id/tool_bar_right_icon">

        <TextView
            android:id="@+id/tool_bar_main_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:textStyle="bold"
            android:text="@string/app_name"
            android:textSize="20sp"
            android:padding="3dp"
            android:textColor="@color/color_whiteText"
            />

        <TextView
            android:id="@+id/tool_bar_sub_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_below="@+id/tool_bar_main_title"
            android:textSize="15sp"
            android:textColor="@color/color_whiteText"
            tools:text="@string/app_name" />

    </RelativeLayout>

    <ImageView
        android:id="@+id/tool_bar_right_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="10dp"
        android:visibility="invisible"
        tools:src="@drawable/ic_info_outline_white_24dp"
        tools:visibility="visible"
        android:contentDescription="@string/back"/>

</RelativeLayout>